storedProcedure:
  name: 'GetBalanceForDetailBookings_OnADate'
  description: 'Returns the balance on a specific date for detail bookings\r\nHow to calculate the balance for detail bookings (PtAccountMovementDetailView - PtTransItem, PtTransItemDetail)\r\n 1. Calculate the real balance\r\n 2. Calculate the delta amount - see where condition\r\n 3. Calculate the Balance = RealBalance - DeltaBalance'